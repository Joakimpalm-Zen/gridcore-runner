The grid runs distributed inference across heterogeneous machines. Each node reports its capabilities and receives work sized to fit. The grid runs distributed inference across heterogeneous machines. Each node reports its capabilities and receives work sized to fit. The grid runs distributed inference across heterogeneous machines. Each node reports its capabilities and receives work sized to fit. The grid runs distributed inference across heterogeneous machines. Each node reports its capabilities and receives work sized to fit. The grid runs distributed inference across heterogeneous machines. Each node reports its capabilities and receives work sized to fit. The grid runs distributed inference across heterogeneous machines. Each node reports its capabilities and receives work sized to fit. The grid runs distributed inference across heterogeneous machines. Each node reports its capabilities and receives work sized to fit. The grid runs distributed inference across heterogeneous machines. Each node reports its capabilities and receives work sized to fit. The grid runs distributed inference across heterogeneous machines. Each node reports its capabilities and receives work sized to fit. The grid runs distributed inference across heterogeneous machines. Each node reports its capabilities and receives work sized to fit. The grid runs distributed inference across heterogeneous machines. Each node reports its capabilities and receives work sized to fit. The grid runs distributed inference across heterogeneous machines. Each node reports its capabilities and receives work sized to fit. Summarize the above in one sentence.